# Provisioned at startup: nobody clicks through the UI to add a datasource,
# and a fresh Grafana is identical every time.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
